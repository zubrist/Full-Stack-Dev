let arr =[ "Apple", "banana", "Orange"]

console.log(arr)

if(arr[0]=="Apple")
    {
        console.log("The first element is  Apple");
    }
else{
    console.log("The first element is not an Apple")
}
// Console Commands 
// arr.length or  arr["length"] to find the length of the array

// arr.push("Guava") - to a new element at the last of the  array
// arr.pop()  - return the last element of the array and removes the element from the array
// arr.indexOf("Element") - to find the index of the element.
//arr.shift()- returns the first element of the array and removes form the array.
// arr.unshift("Element") - adds element at the front of the array